# API Configuration
# Em dev, deixe VITE_API_BASE_URL vazio para usar o proxy do Vite.
VITE_API_BASE_URL=

# Chave de API: deve ser a MESMA que o servidor (FERRESDB_API_KEYS no backend
# ou config.toml api_keys). Sem isso, listar/criar chaves retorna 403.
VITE_API_KEY=

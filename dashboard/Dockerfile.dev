FROM node:20-alpine

WORKDIR /app

# Instala dependências
COPY package*.json ./
RUN npm ci

# Expõe porta do Vite
EXPOSE 5173

# Comando para desenvolvimento (hot reload)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]

# E2E tests: pytest, parallel execution, HTTP client
pytest>=7.0.0
pytest-xdist>=3.0.0
requests>=2.28.0
python-dotenv>=1.0.0

# For test_full_rag_pipeline and test_concurrent_ingestion (local embeddings + optional LLM)
# Install from examples/ingestion and examples/simple_rag if running RAG tests:
#   sentence-transformers  (local embeddings)
#   openai                 (RAG LLM when OPENAI_API_KEY is set)

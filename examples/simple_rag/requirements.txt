# RAG example: FerresDB + LLM
requests>=2.28.0
openai>=1.0.0
anthropic>=0.39.0
python-dotenv>=1.0.0

# Rerankers: CrossEncoder (local) e Cohere (API)
sentence-transformers>=2.2.0
cohere>=4.0.0

# Embeddings (reuso do pipeline de ingestão; instale de ../ingestion se rodar ingest daqui)
# openai, tiktoken já acima; para local: sentence-transformers
